# 🏥 آزادی طب (Azadi-Teb)

**سامانه پذیرش و مدیریت درمانگاه — نسخه ۱.۰.۰**

برنامه دسکتاپ ویندوز با C++ خالص (Win32 API) — بدون هیچ وابستگی خارجی، یک فایل EXE واحد.

---

## ⚠️ راهنمای مدل‌های هوش مصنوعی (مهم — اول این را بخوانید)

اگر شما یک مدل هوش مصنوعی هستید که این مخزن (یا بکاپ آن) به شما داده شده تا روی آن کار کنید:

1. **ابتدا این فایل‌ها را به ترتیب بخوانید:**
   - `README.md` (همین فایل) — معرفی کلی برنامه
   - `docs/PROJECT_GUIDE.md` — ساختار کامل کد، معماری، و ساز و کار هر بخش
   - `docs/CHANGELOG.md` — تاریخچه تغییرات هر نسخه
   - `docs/PROMPT.md` — پرامپت اصلی و تکمیلی که برنامه بر اساس آن ساخته شده

2. **قوانین الزامی:**
   - ❌ **هیچ بخش یا قابلیت موجود را حذف یا دستکاری نکنید** مگر اینکه صریحاً خواسته شده باشد.
   - ✅ فقط قابلیت جدید **اضافه** کنید یا باگ مشخص‌شده را رفع کنید.
   - ✅ بعد از هر تغییر، `docs/CHANGELOG.md` را به‌روز کنید (چه کاری، چرا، در کدام فایل).
   - ✅ سبک کدنویسی موجود (Win32 خالص، فونت Vazirmatn، تم روشن/تیره، RTL فارسی) را حفظ کنید.
   - ✅ خروجی همیشه باید **یک EXE واحد** باشد که روی x86 و x64 و ویندوز 7 تا 11+ کار کند (با `build.sh` بیلد کنید).
   - ✅ بعد از بیلد موفق، EXE جدید را در پوشه `build/` قرار دهید.

3. **برای بیلد:** `./build.sh` (نیاز به `g++-mingw-w64-i686` روی لینوکس) — جزئیات در PROJECT_GUIDE.

---

## ✨ قابلیت‌ها

| بخش | توضیح |
|---|---|
| 🖥️ تمام‌صفحه | اجرای Fullscreen بدون نوار عنوان و منو |
| 🎨 UI مدرن | تم روشن + تم تیره (بدون تداخل رنگ)، دکمه‌های Flat، آیکون‌های وکتوری |
| 🔤 فونت وزیر | فونت Vazirmatn داخل EXE تعبیه شده + نصب خودکار روی سیستم کاربر |
| 🇮🇷 فارسی کامل | راست‌به‌چپ، اعداد فارسی، تقویم جلالی، ساعت دقیق ایران (UTC+3:30) |
| ⏰ ساعت زنده | پایین راست: ساعت لحظه‌ای ایران + تاریخ کامل جلالی |
| 🚪 خروج | دکمه خروج بالا سمت راست |
| 👥 انواع کاربری | پذیرش درمانگاه + پنل مدیریت درمانگاه |
| 🔐 پنل مخفی ادمین | `Ctrl + P + N` در صفحه اصلی → ورود با `prf` / `prf123` |
| 👤 مدیریت کاربر | ساخت کاربر (نام شخص، نام کاربری، رمز، بخش، نوع دسترسی) + جدول کاربران + حذف |
| 🚫 کنترل دسترسی | کاربر پذیرش وارد مدیریت نمی‌شود و بالعکس؛ یوزر تکراری = خطا؛ رمز اشتباه = عدم ورود |
| 🕐 شیفت هوشمند | صبح ۶–۱۴:۳۰ / عصر ۱۴:۳۰–۲۲:۳۰ / شب ۲۲:۳۰–۶ — حالت خودکار با حافظه تیک + انتخاب دستی |
| 🔒 سشن پایدار | با عبور ساعت از مرز شیفت، سشن کاربر قطع **نمی‌شود** — فقط با خروج خود کاربر |
| 🗂️ تب مرورگری | پذیرش = تب مثل مرورگر: باز کردن، بستن، **جدا کردن به پنجره مستقل**؛ نام تب = «پذیرش + بخش» |
| 📝 فرم پذیرش | نام، نام خانوادگی، کد ملی، نام پدر، تاریخ تولد، جنسیت، تلفن، ثابت، آدرس، نوع بیمار، بیمه، بیمه مکمل، نوع نوبت (اورژانس/پرسنلی) — حرکت با **Enter** بین فیلدها |
| 💰 صدور قبض | محاسبه زنده بر اساس **بیمه‌های واقعی ایران** (تأمین اجتماعی، سلامت، نیروهای مسلح، …): سهم بیمه، سهم بیمار، مابه‌التفاوت پایه، سهم سازمان مکمل، تخفیف، پرداختی |
| 🖨️ چاپ واقعی | رسید بیمه / چاپ نسخه / چاپ آخرین قبض — خروجی واقعی روی پرینتر متصل (GDI) |
| ⌨️ F8 | چاپ آخرین قبض با یک کلید |
| 🧮 ماشین حساب | ماشین حساب اختصاصی Always-on-Top با UI خاص — کیبورد، Numpad و ماوس |
| 🛡️ Crash Handler | ثبت خودکار گزارش خطا در `logs/` + پیام دوستانه |
| ⚡ Speed Handler | تشخیص سخت‌افزار ضعیف (رم ۲ گیگ / CPU دو هسته) و کاهش مصرف منابع |
| 📱 ریسپانسیو | مقیاس خودکار UI بر اساس رزولوشن و DPI هر مانیتور |
| 🔄 آپدیت از راه دور | بررسی نسخه از سرور (GitHub raw یا سرور دلخواه در `data/settings.ini`)، دانلود و جایگزینی خودکار |
| 🌐 آماده سرور | لایه داده فایل‌محور و جداشده — قابل ارتقا به دیتابیس/REST بدون تغییر UI |

## 📦 دانلود و اجرا

فایل اجرایی آماده: [`build/AzadiTeb.exe`](build/AzadiTeb.exe)

- یک فایل EXE واحد — **هم روی ویندوز 32 بیتی و هم 64 بیتی** اجرا می‌شود.
- سازگار با ویندوز **7 / 8 / 8.1 / 10 / 11** و جدیدتر.
- نیاز به نصب ندارد؛ کنار EXE پوشه‌های `data/` و `logs/` خودکار ساخته می‌شوند.
- روی سخت‌افزار ضعیف (RAM 2GB / CPU دو هسته‌ای) هم روان کار می‌کند.

### شروع سریع
1. `AzadiTeb.exe` را اجرا کنید (تمام‌صفحه باز می‌شود).
2. در صفحه اصلی `Ctrl + P + N` را نگه دارید → پنل ادمین → ورود با `prf` / `prf123`.
3. کاربر بسازید (مثلاً بخش: «دندانپزشکی»، نوع: پذیرش).
4. خروج (دکمه ✕ بالا راست) → «پذیرش درمانگاه» → ورود با کاربر ساخته‌شده → انتخاب شیفت → ثبت پذیرش.

## 🛠️ بیلد از سورس

```bash
# روی لینوکس (Debian/Ubuntu):
sudo apt install g++-mingw-w64-i686
./build.sh        # خروجی: build/AzadiTeb.exe
```

روی ویندوز با MSYS2/MinGW32 هم همان فرمان‌های داخل `build.sh` قابل اجراست.

## 📂 ساختار مخزن

```
├── src/             ← سورس C++ (Win32 خالص)
├── fonts/           ← فونت Vazirmatn (داخل EXE تعبیه می‌شود)
├── build/           ← خروجی EXE نهایی
├── docs/            ← مستندات کامل (PROJECT_GUIDE / CHANGELOG / PROMPT)
├── update/          ← فایل version.txt برای آپدیت از راه دور
└── build.sh         ← اسکریپت بیلد
```

## 🔄 آپدیت از راه دور — راه‌اندازی سرور

فایل `update/version.txt` در این مخزن (یا هر سرور HTTP دیگر):
```
1.0.1
https://example.com/path/AzadiTeb.exe
```
خط اول = آخرین نسخه، خط دوم = لینک دانلود EXE جدید.
آدرس پیش‌فرض، raw گیت‌هاب همین مخزن است؛ برای تغییر، در `data/settings.ini` کلید `update_url` را تنظیم کنید.

## 🗺️ نقشه راه (نسخه‌های بعدی)

- پایگاه‌داده SQLite/سرور مرکزی + همگام‌سازی چند ایستگاه پذیرش
- ماژول‌های پنل مدیریت: گزارش مالی، آمار پذیرش، مدیریت پزشکان و خدمات
- نوبت‌دهی آنلاین و اتصال به سامانه‌های بیمه (وب‌سرویس)
- چاپ روی پرینتر حرارتی ۸ سانتی فیش‌زن

---
**مجوز:** اختصاصی — © Azadi-Teb
