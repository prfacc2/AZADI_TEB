obj/
*.log
logs/
build/data/
build/logs/
backup_logs/
crashdumps/
build/AzadiTeb_dbg.exe
shot_*.png
shots/
# sandbox-only visual-verification helper scripts (wine + Xvfb screenshots)
shot2.sh
dbgbuild.sh
scripts/*.tar.gz
scripts/AzadiTeb_v*.exe
# v1.4.0 runtime stores (never committed)
data/designs/
data/profile_requests_inbox/
data/profile_requests_outbox/
data/profile_requests_archive/
_design/

# §1.19.0 — sandbox-only HTML designer test artifacts (not part of the EXE build)
node_modules/
package.json
package-lock.json
test_designer.mjs
designer_test.png
build/AzadiTeb_probe.exe

# ============================================================================
#  v2.0.0 — .NET / Avalonia UI + C++ CMake build artifacts
# ============================================================================
# .NET build output (keep only the final build/AzadiTeb.exe)
app/**/bin/
app/**/obj/
build/_publish/
# C++ Core CMake build tree
backend/build/
# sandbox dotnet toolchain env
.dotnet-env.sh
